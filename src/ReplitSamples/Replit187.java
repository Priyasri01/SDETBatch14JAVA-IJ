package ReplitSamples;

import java.util.ArrayList;

public class Replit187 {

    public static void main(String[] args) {

        ArrayList<String> name= new ArrayList<>();

        name.add("hi"); //o
        name.add("yo"); //1
        name.add("sup");//2
        name.add("yolo");//3
        name.add("boop");//4
        System.out.println(name.size());
      /* name.remove(0); // why by using the index 0 it's not remove the elements. rather says index out of bond
        name.remove(2);
       name.remove(3);
       name.remove(4); //same the last index 4  not remove the elements . rather  sayas index out of bond

        System.out.print(name + " ");
        */

    }
}
